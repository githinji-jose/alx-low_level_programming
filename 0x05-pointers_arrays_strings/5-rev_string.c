#include "main.h"

/**
 * rev_string - prints string in reverse
 * @s: pointer of string
 * return: void
 */
void rev_string(char *s)
{
	int i = 0;

	while (s[i])
		i++;

	while (i--)

		return (i);
}
